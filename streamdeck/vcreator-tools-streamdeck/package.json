{
  "name": "vcreator-tools-streamdeck",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/plugin.ts --bundle --platform=node --format=cjs --target=node24 --sourcemap --outfile=jp.vcreatortools.streamdeck.sdPlugin/bin/plugin.cjs",
    "check": "tsc --noEmit",
    "package": "npm run check && npm run build && streamdeck validate jp.vcreatortools.streamdeck.sdPlugin --no-update-check && streamdeck pack jp.vcreatortools.streamdeck.sdPlugin --force --no-update-check --no-file-list --output ../../dist/electron"
  },
  "dependencies": {
    "@elgato/streamdeck": "2.1.1"
  },
  "devDependencies": {
    "@elgato/cli": "^1.8.0",
    "esbuild": "^0.25.9",
    "typescript": "7.0.2"
  }
}
