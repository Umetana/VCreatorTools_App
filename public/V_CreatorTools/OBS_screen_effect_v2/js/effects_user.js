/**
 * ユーザー自作エフェクト登録用ファイル
 * 
 * 公式のアップデートで js/effects_list.js が上書きされても、
 * このファイルに記述した内容は保持されます。
 */
const USER_EFFECTS = [
    // { "id": "your_custom_effect", "name": "自作エフェクト名" },
];

if (typeof module !== 'undefined' && module.exports) {
    module.exports = USER_EFFECTS;
}
