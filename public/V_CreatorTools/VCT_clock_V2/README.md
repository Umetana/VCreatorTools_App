# VCT Clock V2

OBS Studio向けのStandaloneカスタマイズ時計です。ローカルファイルとしてそのまま動作します。

## 主な機能

- 8つの公式プリセット
- 名前を付けて保存できる3つのマイプリセットスロット
- 秒・日付・12/24時間表記・コロン点滅の切替
- 文字、光彩、背景、角丸、文字間隔、黒縁の調整
- LocalStorageによる設定復元と旧V1設定の自動移行
- 任意ローカルフォントの指定
- 設定と3スロットをまとめたJSONバックアップ／復元
- OBS対話画面向けのページ内カラーピッカーとフォント選択ダイアログ
- Tailwind CDN不使用。標準状態はネットワーク接続不要

## OBSへの追加

1. OBSのソースから「ブラウザ」を追加します。
2. 「ローカルファイル」を有効にし、このフォルダの`index.html`を選択します。
3. 幅を`1920`、高さを`1080`にします。
4. 「相互作用」で設定画面を開き、デザインを調整します。
5. 「設定を完全に保存」を押します。
6. OBSプレビューでAltキーを押しながらクロップし、左上の340×120の時計領域だけを残します。

## フォントと通信

「標準（オフライン）」はOS搭載フォントを使用し、通信しません。名前に「オンライン」と表示されたGoogle Fontsは、選択した書体だけをその時点で読み込みます。取得できない場合は自動的に標準フォントへ戻ります。

「任意ローカルフォント」では、PCにインストール済みのフォント名を入力できます。対象フォントがない環境では標準フォントへフォールバックします。

## プリセット

1. サイバー・ネオン
2. ミニマル・モダン
3. レトロ・ポップ
4. シック・和モダン
5. 放送局・ニュース
6. パステル・ドリーム
7. LED・電光掲示板
8. クリーン・モノクロ

プリセット適用後も各項目を自由に調整できます。設定は保存ボタンを押した時点で最終設定として保存されます。

## 設定JSON

「JSON入出力」は画面内に設定JSONを表示します。現在設定と保存済みの3スロットを`vct-clock.backup.v1`形式で生成し、全選択、コピー、貼り付け、検証後の適用、クリアを行えます。JSONに含まれていないスロットは変更しません。

ファイル読込／書出しも同じ画面から利用できますが、補助機能です。OBS内ではファイル保存が制限されるため、テキストのコピー／貼り付けを使用するか、ファイル書出しを通常ブラウザーまたはVCreatorTools Appから行ってください。ファイル読込後は内容を確認し、「JSONを適用」を押すまで設定へ反映されません。

## 旧設定の移行

旧版の`obs_clock_config`と`obs_clock_slot_1`～`3`が存在する場合、V2データがまだないときに自動で読み込みます。正常な項目をV2形式へ移行し、旧データは削除しません。

## ライセンス

MIT License。個人・法人、商用・非商用を問わず利用できます。

## 更新履歴

- **v2.1.0-beta.2**（2026-08-18）
  - 画面内のJSON入出力パネルを追加
  - コピー／貼り付けを主導線とし、ファイル読込／書出しを補助機能へ変更
  - OBSでのファイル保存制限に合わせて、ファイル読込後の確認・適用手順を分離
- **v2.1.0-beta.1**（2026-08-18）
  - デザインプリセットを8種類へ拡張
  - 任意ローカルフォント、設定JSONのバックアップ／復元を追加
  - `_vct_lib`のカラーピッカーと選択ダイアログを採用
- **v2.0.0**（2026-08-10）
  - Standalone版V2として、ローカルCSSとオフライン標準フォントへ移行
  - 既存4プリセット、3保存スロット、旧LocalStorage設定の移行に対応
