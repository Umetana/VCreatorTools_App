# User Gadget Sample

VCreatorTools Appへ持ち込むガジェットの最小例。`manifest.json`、操作ページ、OBS向け表示ページを含む。

- Standalone: ページ単体で表示する
- Sync: 同じOriginのページ間をBroadcastChannelで同期する
- Server API: 使用しない

Folder名、manifestの名前とversion、BroadcastChannel名、localStorage keyは複製後に固有名へ変更する。
